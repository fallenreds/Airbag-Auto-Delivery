# Перенос данных из старого бота в новую систему

Старая версия (ветка `master`, боевая до сих пор) хранит всё в одном SQLite-файле
`AirbagDatabase.db`. Новая (`v2`) — в Django-моделях `core_*`. Схемы несовместимы:
заказ там держал состав одной строкой питоновского `repr`, клиент — пароль
открытым текстом, суммы — в гривнах, а не в копейках.

Переносит данные management-команда
[`import_legacy_db`](../backend/core/management/commands/import_legacy_db.py).

## Как запускать

```bash
cd backend

# 1. Превью — база не меняется, печатается «что и сколько приедет»
python manage.py import_legacy_db /path/AirbagDatabase.db

# 2. Резервная копия перед записью
cp db.sqlite3 db.sqlite3.bak-$(date +%Y%m%d)

# 3. Запись
python manage.py import_legacy_db /path/AirbagDatabase.db --apply
```

Превью — режим по умолчанию: без `--apply` не пишется ничего. Считается оно не
«на глаз», а настоящей записью внутри транзакции, которая затем откатывается —
поэтому цифры превью совпадают с боевым прогоном (это проверяется тестом).

### Флаги

| Флаг | Зачем |
|---|---|
| `--apply` | действительно писать в базу |
| `--only clients,orders` | часть секций: `clients`, `orders`, `carts`, `discounts`, `templates`, `visitors` |
| `--since 2026-01-01` | только заказы не раньше даты |
| `--limit-orders 50` | пробный прогон на последних N заказах |
| `--with-passwords` | перенести пароли (в старой базе они открытым текстом; по умолчанию пароль ставится неиспользуемым) |
| `--no-remonline-check` | не сверять активные заказы с RemOnline (по умолчанию сверяются) |
| `--verbose-skips` | печатать каждую пропущенную запись, а не только сводку |

Импорт **идемпотентен**: повторный запуск на той же базе ничего не дублирует.
Можно гонять по секциям, править и повторять.

## Что во что переезжает

| Старая таблица | Новая модель | Примечания |
|---|---|---|
| `client` | `Client` | телефон нормализуется к `+380XXXXXXXXX`; почты у старых клиентов нет |
| `orders` | `Order` | `date` восстанавливается отдельным UPDATE — у поля `auto_now_add` |
| `orders.goods_list` | `OrderItem` | строка `repr` разбирается; `good_id` — это remonline-id товара |
| `shopping_cart` | `Cart` + `CartItem` | группируется по `telegram_id` |
| `discounts` | `Discount` | `month_payment` умножается на 100 (гривны → копейки) |
| `template` | `Template` | |
| `visitors` | `BotVisitor` | |
| `client_updates`, `order_updates` | — | очереди событий, переносить нечего |
| «бонусные» строки `orders` | — | не заказы, а ручная выдача скидки; переносятся отдельно через `add-bonus` |

## Номера заказов сдвигаются на 100 000

Импортированный заказ получает `id = 100000 + legacy_id`. Причина не
косметическая: кнопки под сообщениями старого бота живут в чатах годами и несут
в `callback_data` его номера (`delete_order/13207`). После переноса токена
`@AirBagAD_bot` они приходят уже в новый бот. Без сдвига такое нажатие попало бы
в чужой заказ с тем же номером; со сдвигом легаси-номера не пересекаются с
диапазоном новой системы, и кнопка просто не находит заказ (а бот отвечает, что
она устарела).

Следствие: заказы новой системы после импорта продолжат нумерацию с 113 208.

## Бонусные начисления не импортируются

Шесть строк в боевой `orders` — не заказы, а ручная выдача скидки: `description`
и `nova_post_address` равны `BONUS`, единственная позиция — служебный товар
`34459054`, а `count` в ней равен сумме подарка в гривнах (10 000–20 010).
Старый фронт этот товар прятал. Как заказ такая строка выглядит дико: «Товар
#34459054 × 20010» на 0 грн от 2023 года.

Импортёр их пропускает и показывает отдельной строкой превью — сумму видно, и
перенести её можно осознанно, через `POST /clients/{id}/add-bonus/`.

## Суммы старых заказов обнуляются

Скидка считается по сумме **прошлого месяца**, поэтому у заказов старше двух
месяцев `subtotal_minor` и `grand_total_minor` ставятся в ноль: их суммы
восстановлены по сегодняшнему прайсу, скидка утрачена, а выбывшие из каталога
товары идут по нулю — в агрегате это только шум. Позиции и цены в `OrderItem`
остаются, история заказа в интерфейсе не страдает.

Превью показывает отдельно: сколько заказов и на какую сумму попадут в расчёт
скидки.

## Сверка активных заказов с RemOnline

Дамп старой базы всегда отстаёт от жизни: пока его снимали и переносили, часть
заказов успела закрыться. На копии от 20.08.2026 таких оказалось **4 из 11**
открытых. Без сверки `order_event_handler` на первом же проходе увидел бы в
RemOnline «Закрито», создал событие `FINISHED` — и клиент получил бы
«Дякуємо за замовлення» второй раз, потому что старая система уже это сказала.

Поэтому после импорта команда один раз спрашивает RemOnline про активные заказы
и закрывает те, что там уже завершены. Сбой сети импорт не ломает — в отчёте
появится строка, а заказы останутся открытыми. Отключается флагом
`--no-remonline-check`.

## Решения, которые стоит знать

- **Новая база — источник истины.** Если клиент уже есть (совпал `telegram_id`,
  затем `id_remonline`, затем телефон), импорт только дополняет **пустые** поля
  и никогда не перебивает заполненные.
- **Даты старой базы — UTC.** Старый бот писал их через SQLite `datetime('now')`,
  а это UTC без таймзоны.
- **Заказы приезжают со статусом `SYNCED`.** Они уже заведены в RemOnline, и
  `PENDING` заставил бы систему завести дубли.
- **Суммы восстанавливаются по текущему прайсу.** Старый заказ не хранил ни
  итога, ни скидки, поэтому `grand_total` равен `subtotal`, а `discount_percent`
  остаётся пустым. Историческая аналитика по деньгам из этих чисел
  недостоверна — это оценка, а не факт.
- **Пароли по умолчанию не переносятся.** Вход в новую систему — по почте,
  которой у старых клиентов нет; логин сохраняется как справочная информация.
- **Позиция удалённого товара сохраняется снимком** (`good=None`, title
  `Товар #<remonline id>`, цена 0) — заказ не должен терять состав из-за того,
  что товар убрали из каталога. Позиция корзины на такой товар, наоборот,
  пропускается: у `CartItem` снимка нет, только FK.

## Проверка на реальной копии прода (21.08.2026)

Копия боевой базы старого бота, последний заказ — 20.08.2026 16:47:

```
секция       в источнике   создать  дополнить  пропустить
clients              173       171          0           2
orders              3918      3918          0           0
carts                325       325          0           0
discounts              4         0          0           4
templates              2         2          0           0
visitors             511       509          0           2
```

Заказы за 01.02.2023 — 20.08.2026, 12 092 позиции, из них 7 (2 товара) уже
удалены из каталога. Четыре ступени скидок уже заведены в новой базе, поэтому
пропущены. Прогон занимает ~7 секунд, повторный не создаёт ничего.
